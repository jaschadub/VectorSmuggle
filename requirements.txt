# Core dependencies (LangChain 1.x stack)
langchain>=1.2.10,<2.0.0
langchain-core>=1.2.15,<2.0.0
langchain-community>=0.4.1,<1.0.0
langchain-openai>=1.1.10,<2.0.0
langchain-text-splitters>=1.1.1,<2.0.0
openai>=1.50.0

# Ollama fallback support
ollama>=0.1.7,<1.0.0

# Vector stores
faiss-cpu>=1.8.0,<2.0.0
qdrant-client>=1.7.0,<2.0.0
# pgvector backend (vector_backends.pgvector_backend). psycopg 3.x is
# required; pgvector provides the type adapter that registers the
# `vector` column type with psycopg.
psycopg[binary]>=3.1,<4.0
pgvector>=0.3,<1.0
# Pinecone: pinned to v2.x because code uses legacy pinecone.init() API.
# Upgrading to v3+ requires migrating database_adapters/pinecone_adapter.py
# and scripts/{embed,query}.py to the new Pinecone class-based client.
pinecone-client>=2.2.4,<3.0.0

# Document processing
pypdf>=3.17.4,<7.0.0
python-docx>=1.1.0,<2.0.0
openpyxl>=3.1.2,<4.0.0
python-pptx>=0.6.23,<2.0.0
beautifulsoup4>=4.12.2,<5.0.0
PyYAML>=6.0.1,<7.0.0
extract-msg>=0.47.0,<1.0.0

# Scientific computing for steganography
numpy>=1.26.0,<3.0.0

# Network and evasion dependencies
requests>=2.32.5,<3.0.0
fake-useragent>=1.4.0,<3.0.0
schedule>=1.2.0,<2.0.0
psutil>=5.9.6,<8.0.0

# Development and testing
ruff>=0.15.2,<1.0.0
bandit>=1.7.5,<2.0.0

# Query enhancement dependencies
networkx>=3.2.1,<4.0.0
spacy>=3.7.2,<4.0.0
scikit-learn>=1.3.2,<2.0.0
